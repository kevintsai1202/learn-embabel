# 第一單元插圖 AI 生圖提示詞手冊 (第一單元專用)

本文件收錄了第一單元中兩張插圖的 AI 生圖提示詞（適用於支援中英文文字生成的現代生圖模型，如 Imagen 2/3、DALL-E 3 等）。

---

## 1. 把 agentic AI 放進 JVM 工程邊界
* **PNG 檔名**：`day1-u1-jvm-agent.png`
* **Use case**: scientific-educational
* **Asset type**: teaching website unit hero image
* **Primary request**: Create a premium technical illustration demonstrating keeping agentic AI inside existing JVM and Spring engineering boundaries. Include clear text labels.
* **Style**: premium editorial 3D/flat hybrid illustration, refined technical education style.
* **Composition**: wide 16:9. A secure JVM/Spring engineering boundary zone on one side (labeled "JVM Boundary" and "Spring Boot"), housing stable services. Connected via a controlled gate bridge to the other side where a Large Language Model node (labeled "LLM") and authorized system tools (labeled "Approved Tools") reside. Clear flow arrows and labels indicating secure integration.
* **Constraints**: no watermarks, no realistic faces.

---

## 2. 控制權邊界
* **PNG 檔名**：`day1-u1-boundary.png`
* **Use case**: scientific-educational
* **Asset type**: teaching website unit diagram image
* **Primary request**: Create a technical diagram illustrating the concept of control boundary in an AI agent system, where a central planner manages the overall workflow, and the LLM is strictly confined inside actions. Include clear text labels.
* **Style**: premium editorial 3D/flat hybrid illustration, refined technical education style.
* **Composition**: wide 16:9. A large, prominent workflow engine module (labeled "Central Planner") orchestrating a pipeline of three consecutive steps (labeled "Action 1", "Action 2", and "Action 3"). A localized, glowing LLM node (labeled "LLM") is enclosed strictly inside the containers of Action 2 and Action 3, showing it only runs locally as a sub-process rather than directing the main flow.
* **Constraints**: no watermarks, no realistic faces.
